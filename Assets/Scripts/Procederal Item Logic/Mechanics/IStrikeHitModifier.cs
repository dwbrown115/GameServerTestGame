// Deprecated legacy interface retained only to avoid compile errors in serialized Unity metadata.
// New implementations should use Mechanics.IPrimaryHitModifier.
namespace Mechanics
{
    internal interface IStrikeHitModifier { }
}
